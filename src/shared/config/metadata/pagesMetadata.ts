import { appName } from '../../const/system';

import { icons } from './pagesIcons';

export const pagesMetadata = {
  main: {
    icons,
    title: `Главная | ${appName}`,
    description: '',
  },
};
